syntax = "proto3";
package tokenity.blog.v1;

import "gogoproto/gogo.proto";
import "tokenity/blog/v1/params.proto";
import "tokenity/blog/v1/post.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/coreators/tokenity/x/blog/types";

// GenesisState defines the blog module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
  repeated Post postList = 2 [(gogoproto.nullable) = false];
  uint64 postCount = 3;
  // this line is used by starport scaffolding # genesis/proto/state
}
